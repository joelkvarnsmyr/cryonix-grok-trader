import Layout from '@/components/Layout';
import KPIDashboard from '@/components/KPIDashboard';

const KPI = () => {
  return (
    <Layout>
      <KPIDashboard />
    </Layout>
  );
};

export default KPI;