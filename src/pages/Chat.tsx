import Layout from '@/components/Layout';
import ChatInterface from '@/components/ChatInterface';

const Chat = () => {
  return (
    <Layout>
      <ChatInterface />
    </Layout>
  );
};

export default Chat;